# Split each line into it's own array (they all all equal length)
# Iterate through each line and then do a for loop on each '@', check the index before and after and then
# check the before, after AND equal index on the previous and next array index entry.

# For each additional '@' found, add 1 to the count. If the total is greater or equal to 5 (including the original '@' then do not add to the total count.)
# Otherwise, add 1 to the total count.